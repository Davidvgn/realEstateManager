<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/operations_menu_add"
        android:icon="@drawable/ic_baseline_add_24"
        android:title="@string/add_realestate"
        app:showAsAction="ifRoom|collapseActionView"

        />

    <!-- todo david : rendre visible dans les détails des biens -->
    <item
        android:id="@+id/operations_menu_edit"
        android:icon="@drawable/ic_baseline_mode_edit_24"
        android:title="@string/edit_realestate"
        android:visible="false"
        app:showAsAction="ifRoom|collapseActionView"
        />

    <item
        android:id="@+id/operations_menu_search"
        android:icon="@drawable/ic_baseline_search_24"
        android:title="@string/search"
        app:actionViewClass="android.widget.SearchView"
        app:showAsAction="ifRoom|collapseActionView"
        />
</menu>